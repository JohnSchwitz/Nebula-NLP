import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#vue-form') // associated with HTML element with 'id="vue-form"' 
// mounts Vue component to this element
